{
  "name": "jscad-surface",
  "version": "1.0.1",
  "description": "Create 3D surfaces from height map data",
  "main": "src/index.js",
  "scripts": {
    "coverage": "nyc --all --reporter=html --reporter=text npm test",
    "lint": "standard './**/*.js'",
    "test": "ava 'tests/**/*.test.js' --verbose --timeout 2m"
  },
  "keywords": [
    "jscad",
    "image",
    "surface",
    "mesh"
  ],
  "author": "z3dev",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "image-decode": "1.2.2",
    "ava": "1.4.0",
    "nyc": "15.0.0",
    "standard": "14.3.1"
  }
}
